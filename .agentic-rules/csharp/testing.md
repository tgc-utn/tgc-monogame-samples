# C# testing guidelines

## Current state

There is no dedicated test project in this repository. `dotnet test` passes vacuously.

Until a test project exists, manually exercise every change through the
viewer to verify the affected sample loads and runs without errors.
